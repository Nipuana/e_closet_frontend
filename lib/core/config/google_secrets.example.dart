// Template for google_secrets.dart. Copy this file to `google_secrets.dart`
// (same folder) and fill in your real client IDs. google_secrets.dart is
// gitignored so the values never get committed.
class GoogleSecrets {
  GoogleSecrets._();

  /// OAuth 2.0 **Web** client ID. Must match the backend's GOOGLE_CLIENT_ID.
  static const String serverClientId = '';

  /// OAuth 2.0 **iOS** client ID. Leave empty for Android-only builds.
  static const String iosClientId = '';
}
